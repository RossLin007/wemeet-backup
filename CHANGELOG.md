# 更新日志 (Changelog)

本文件记录项目的显著变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2026-09-14] 新增运行日志：main.py 执行历史与运行结果可查

### 新增

- **每次运行自动留档**：新模块 `tencent_meeting/runlog.py`。终端输出照常实时显示，同时双写（tee）到 `logs/run-<时间戳>-<模式>.log`；文件名即执行历史，模式含 incremental/full/single/batch/clean/clean-apply。
- **结果摘要索引 `logs/run_index.jsonl`**：每次运行追加一行机器可读摘要——起止时间、耗时、模式、状态（ok/interrupted/crash/exit(n)）、处理/成功/失败数、视频/音频/转写计数、失败明细（记录、原因，至多 50 条）、清理模式统计（deletable/skipped/deleted/freed_bytes）。`tail logs/run_index.jsonl` 一行看懂任意一次运行结果。
- **失败采集**：增量主循环把 `result.ok=False` 的记录（含 transcript/video/audio 各状态）与被熔断跳过（fails≥3）的记录写入当次日志与摘要——正是此前排查「视频没下载下来」时缺失的信息。
- **细节**：stdout/stderr 双捕获（崩溃 traceback 留档）；下载器的 `\r` 单行刷新进度不落盘（避免日志膨胀），只保留结果行；异常路径 try/finally 保证崩溃也落摘要；清理模式经 `run_cleaner(stats=...)` 回填统计。
- 敏感红线：日志含会议标题/标识符，`logs/` 已加入 `.gitignore` 与 AGENTS.md 红线清单。

### 为什么

排查「协同之舞-思维方式的力量 视频没下载下来」时发现：所有输出仅打印到终端即逝，无法回查历史运行的失败记录与原因，只能靠状态清单反推。

### 验证

- 39 个单元测试通过（新增 TestRunLog 4 例：\r 进度抑制与终端透传、日志文件与摘要索引字段、崩溃收尾恢复流、清理 stats 回填）。
- 实跑 `--clean` 干跑与常规增量：`logs/` 生成对应日志，`run_index.jsonl` 摘要含 deletable=58/skipped=1 等清理统计。

## [2026-09-14] 修复续传 403 死循环 + 云端改名自动迁移目录

### 修复

- **续传被 403 拒绝时原地重试死循环**：媒体直链中断后 `.tmp` 残留，下次运行携带 Range 续传会被 COS 以 403 直接拒绝（token 失效/风控），原实现仍按同一 URL 重试 5 次并永久保留无用的 `.tmp`。现检测到 403 且存在续传偏移时，丢弃 `.tmp` 改为整段 GET 重试一次（链接尚有效即可救回）；整段仍被拒则立即放弃并清理 `.tmp`。实测案例：协同之舞-开营仪式_讨论组 02 纯音频的 2MB `.tmp` 反复 403 无法推进。
- **云端会议改名产生同标识符双目录**：主办方改名后（如 协同之舞 → 协同之舞-思维方式的力量），备份按新主题另建目录、旧目录残留，令清理模式的「本地目录唯一定位」失效，相关记录永远进不了待删清单。现 `process_single_backup` 建目录前检测同标识符的旧主题目录（唯一时）自动迁移改名跟随云端。

### 运维记录（数据操作，非代码变更）

- 人工合并清理了历史 10 组改名产生的重复目录：旧目录均为劣质副本（空转写/占位封面），更全文件（真封面等）已并入当前主题目录后删除旧目录，复扫重复标识符归零。
- 协同之舞-思维方式的力量（新录制）首备时云端仍在转码，直链为空连续失败触发熔断；转码完成后重置状态重跑，3 条子记录的视频/音频已全部补齐。

### 验证

- 35 个单元测试通过（新增 4 例：续传 403 丢弃 .tmp 改整段重试、整段仍 403 放弃并清理 .tmp、唯一旧目录自动迁移、目标存在/多旧目录歧义时不迁移）。

## [2026-09-13] 新增线上录制清理模式（备份完成后释放云存储空间）

### 新增

- **清理模式 `--clean`**：删除线上"有视频且本地已完整备份"的云录制记录，释放腾讯会议云存储。默认**干跑**只打印待删清单与预计释放空间；`--apply` 才真删且需交互确认（输入待删条数，防误触）；`--yes` 供脚本化跳过确认；`--limit N` 限制本次删除数。待删清单按旧→新排序，`--limit` 截断时优先删最旧记录。
- **删除接口 `TencentMeetingClient.delete_record()`**：`POST /wemeet-tapi/v2/meetlog/dashboard/delete-record-info`，载荷 `{"uni_record_ids": [...]}`，响应 `code == 0` 判定成功。为网页端「我的录制 → 管理 → 删除」同源接口，端点与载荷由前端 JS 静态分析（webpack chunk 反解）+ 无效 ID 无破坏探测确认。
- **可删判定（全部满足，缺一跳过并给出原因）**：① 线上有视频（沿用 record_type 判定）；② 状态清单 `is_complete`；③ 本地实物二次校验——备份目录按 `主题_标识符` 唯一定位，视频/转写/音频文件按当前配置逐一在盘且非空（防状态与磁盘不一致，如手工误删本地文件）；④ 线上 `allow_delete` 为真。合集（shared-record-middle）以父级为删除单位（删父级连带全部子记录与分享链接），须**全部子记录**通过校验。
- **执行保护**：逐条删除间隔 0.8s；连续 3 次失败自动熔断（防 Cookie 失效/风控时空转）；每删一条立即原子写回状态并打 `deleted_online`/`deleted_at` 审计标记；追加删除日志 `downloads/.deletion_log.jsonl`（标识符/主题/结果/线上占用，位于 gitignored 目录）。本地备份文件永不删除。
- 列表标准化字典增补 `allow_delete` 与 `cloud_size`（线上占用字节，取列表原始字段 `size`，用于估算释放空间）。清理分支在备份调度前短路，不解析媒体直链（省去逐条 download/meeting 调用）。

### 为什么

线上云存储将满，而全部有视频记录已备份到本地；但删除不可恢复，必须把"本地实物校验 + 默认干跑 + 显式确认 + 审计日志"做成硬性关卡后才可自动化批量删除。

### 验证

- 31 个单元测试通过（新增 TestCleaner 9 例：可删筛选与排序/本地视频缺失跳过/状态未完成与目录歧义跳过/无视频与 allow_delete=false 忽略/合集须全子齐备/干跑零删除/apply 标记状态并写日志/确认不匹配取消/连续失败熔断）。
- 无效 ID 探测：删除接口对不存在 ID 幂等返回 `code=0`，证明端点与 Cookie 鉴权有效且未命中任何真实记录。
- 真实 cookie 干跑：56 条可删（预计释放 52.94 GB）+ 2 条暂不可删（合集容器状态未对账、子记录本地目录缺失），56+2=58 与人工核对报告的有视频记录总数一致。

## [2026-09-13] 新增纯音频备份 + 媒体直链下载

### 新增

- **纯音频文件备份**：补齐网页端"另存为"菜单 4 种下载中缺失的"纯音频文件"。走 `GET /wemeet-cloudrecording-webapi/v1/download/meeting`（与菜单同源）返回的 `audio_link`（`.m4a` 带 token 直链），按 `audio_{标识符}.m4a` 保存，复用下载器的断点续传。由 `download_audio` 配置开关控制（默认关闭）。
- **媒体直链优先下载（视频同样受益）**：视频不再优先启动无头浏览器拦截，而是直接使用 `download/meeting` 返回的 `link` 直链流式下载（免浏览器、可断点续传）；直链解析失败或下载失败时回退原有的浏览器拦截流程。直链按 COS 资源 ID（= 子记录 `recording_id` 口径）分发给单条记录或合集的各子记录。

### 增量状态

- 状态清单每条记录新增 `audio` 字段（`done`/`skipped`/`unknown`/`failed`/`off`），语义与 `video` 一致；`off` 表示配置未启用音频下载。
- 中途启用 `download_audio` 时自动将历史 `off` 重置为待对账（`unknown`），保证已完成的记录也能补上音频；旧版状态文件无 `audio` 键的记录按 `unknown` 处理，会在下次运行时对账一次。
- 磁盘引导（bootstrap）识别 `audio_{标识符}.*` 已有音频文件。

### 验证

- 22 个单元测试通过（新增直链解析/分发/失败回退/合集子记录分发/audio 状态映射 5 个用例）。
- 遍历干跑核对：1851 条记录、标识符唯一、58 条有视频，与改前一致。
- 实测下载"协同之舞-品德成功论_录制1"纯音频 24.7MB（M4A，有效 ISO Media 容器），与已有视频同目录存放。

### 说明

- 网页端"逐字稿文本 / 时间轴文本"两种下载由既有转写导出覆盖（`txt`/`md` ≈ 逐字稿，`srt` ≈ 时间轴字幕）。

## [2026-09-13] 新增 AGENTS.md 代理协作约定

### 文档

- 新增 `AGENTS.md`：约定**所有行为性变更须记录 CHANGELOG 并及时提交 git**；附提交前自测要求（pytest 全过 + 遍历类改动干跑核对）、项目速览、敏感信息红线（Cookie/会议数据严禁入库）、关键设计决策（列表遍历不做提前停止、page_size=30，勿回退）。

## [2026-09-13] 增量遍历修复 + 分页提速

### 修复

- **增量模式补不上历史未完成记录（严重）**：原"某一整页记录均已登记即提前停止翻页"的优化存在根本缺陷——判定依据是"已登记"而非"已备份完成"，而两者并不等价（转写已导出但视频下载中断的记录处于已登记、未完成状态）。历史中任意一整页已完成记录都会挡住更早的未完成记录，导致增量模式永远不会重试它们（实测：34 个缺失视频全部被挡在门外）。现移除该提前停止逻辑，列表始终完整翻页；去重开销由既有的"已完成直跳"（逐条跳过、不调接口、不启浏览器）承担。
- **连带修复**：状态清单从磁盘引导时产生的大量 `video=unknown` 记录（磁盘扫描无法区分"本身无视频"与"下载失败"），此前同样被提前停止挡住而无法对账修正，现会在每次运行中被正确修正为 `skipped`/`done`。

### 优化

- **列表分页提速**：`my-record-list` 分页大小由 20 提升至 30（网页端单页上限，10/20/30 可选）。已验证接口真实支持 30 条/页、相邻页无重叠且按 `uni_record_id` 严格降序连续；以 1851 条记录计，翻页请求由 93 次降至 62 次（约 -1/3）。

## [2026-09-13] (a498280) 默认增量备份模式 + 大文件断点续传

- 默认增量备份：`downloads/.backup_state.json` 状态清单跟踪每条记录的转写/视频状态，已完成记录跳过处理；首次运行自动扫描本地已有备份引导生成清单。
- 大文件断点续传：视频下载中断保留 `.tmp` 进度，携带 Range 头续传（最多 5 次），重试耗尽后下次运行接着下。
- 失败熔断：连续失败 3 次的记录增量模式不再自动重试，`--full` 强制重试。

## [2026-08-01] (be754da) 初始版本

- 腾讯会议自动备份工具（wemeet-backup）：账号全量自动遍历、合集（shared-record-middle）递归展开、云录制视频与封面下载、转写逐字稿多格式导出（md/txt/srt/json）、CLI 单条/批量/全自动三种输入模式。
