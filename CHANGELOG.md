# 更新日志 (Changelog)

本文件记录项目的显著变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2026-09-13] 新增纯音频备份 + 媒体直链下载

### 新增

- **纯音频文件备份**：补齐网页端"另存为"菜单 4 种下载中缺失的"纯音频文件"。走 `GET /wemeet-cloudrecording-webapi/v1/download/meeting`（与菜单同源）返回的 `audio_link`（`.m4a` 带 token 直链），按 `audio_{标识符}.m4a` 保存，复用下载器的断点续传。由 `download_audio` 配置开关控制（默认关闭）。
- **媒体直链优先下载（视频同样受益）**：视频不再优先启动无头浏览器拦截，而是直接使用 `download/meeting` 返回的 `link` 直链流式下载（免浏览器、可断点续传）；直链解析失败或下载失败时回退原有的浏览器拦截流程。直链按 COS 资源 ID（= 子记录 `recording_id` 口径）分发给单条记录或合集的各子记录。

### 增量状态

- 状态清单每条记录新增 `audio` 字段（`done`/`skipped`/`unknown`/`failed`/`off`），语义与 `video` 一致；`off` 表示配置未启用音频下载。
- 中途启用 `download_audio` 时自动将历史 `off` 重置为待对账（`unknown`），保证已完成的记录也能补上音频；旧版状态文件无 `audio` 键的记录按 `unknown` 处理，会在下次运行时对账一次。
- 磁盘引导（bootstrap）识别 `audio_{标识符}.*` 已有音频文件。

### 验证

- 22 个单元测试通过（新增直链解析/分发/失败回退/合集子记录分发/audio 状态映射 5 个用例）。
- 遍历干跑核对：1851 条记录、标识符唯一、58 条有视频，与改前一致。
- 实测下载"协同之舞-品德成功论_录制1"纯音频 24.7MB（M4A，有效 ISO Media 容器），与已有视频同目录存放。

### 说明

- 网页端"逐字稿文本 / 时间轴文本"两种下载由既有转写导出覆盖（`txt`/`md` ≈ 逐字稿，`srt` ≈ 时间轴字幕）。

## [2026-09-13] 新增 AGENTS.md 代理协作约定

### 文档

- 新增 `AGENTS.md`：约定**所有行为性变更须记录 CHANGELOG 并及时提交 git**；附提交前自测要求（pytest 全过 + 遍历类改动干跑核对）、项目速览、敏感信息红线（Cookie/会议数据严禁入库）、关键设计决策（列表遍历不做提前停止、page_size=30，勿回退）。

## [2026-09-13] 增量遍历修复 + 分页提速

### 修复

- **增量模式补不上历史未完成记录（严重）**：原"某一整页记录均已登记即提前停止翻页"的优化存在根本缺陷——判定依据是"已登记"而非"已备份完成"，而两者并不等价（转写已导出但视频下载中断的记录处于已登记、未完成状态）。历史中任意一整页已完成记录都会挡住更早的未完成记录，导致增量模式永远不会重试它们（实测：34 个缺失视频全部被挡在门外）。现移除该提前停止逻辑，列表始终完整翻页；去重开销由既有的"已完成直跳"（逐条跳过、不调接口、不启浏览器）承担。
- **连带修复**：状态清单从磁盘引导时产生的大量 `video=unknown` 记录（磁盘扫描无法区分"本身无视频"与"下载失败"），此前同样被提前停止挡住而无法对账修正，现会在每次运行中被正确修正为 `skipped`/`done`。

### 优化

- **列表分页提速**：`my-record-list` 分页大小由 20 提升至 30（网页端单页上限，10/20/30 可选）。已验证接口真实支持 30 条/页、相邻页无重叠且按 `uni_record_id` 严格降序连续；以 1851 条记录计，翻页请求由 93 次降至 62 次（约 -1/3）。

## [2026-09-13] (a498280) 默认增量备份模式 + 大文件断点续传

- 默认增量备份：`downloads/.backup_state.json` 状态清单跟踪每条记录的转写/视频状态，已完成记录跳过处理；首次运行自动扫描本地已有备份引导生成清单。
- 大文件断点续传：视频下载中断保留 `.tmp` 进度，携带 Range 头续传（最多 5 次），重试耗尽后下次运行接着下。
- 失败熔断：连续失败 3 次的记录增量模式不再自动重试，`--full` 强制重试。

## [2026-08-01] (be754da) 初始版本

- 腾讯会议自动备份工具（wemeet-backup）：账号全量自动遍历、合集（shared-record-middle）递归展开、云录制视频与封面下载、转写逐字稿多格式导出（md/txt/srt/json）、CLI 单条/批量/全自动三种输入模式。
